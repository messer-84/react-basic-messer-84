const selectedbookReducer = (state = null, action) => {
  console.log(action);
  switch (action.type) {
    case 'BOOK_SELECT':
      return state;
    default:
      return state;
  }
};

export default selectedbookReducer;